"""Version 1 of the Agent <-> Collector wire contract.

Breaking changes get a new ``v2`` package alongside this one rather than a
modification here, so the Collector can accept both during an Agent fleet
rollout (see ``docs/architecture/00-project-initialization.md`` §9.3).
"""
