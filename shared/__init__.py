"""Shared library imported by both the Agent and the Collector.

This package has no dependency on ``agent`` or ``collector`` — see
``docs/architecture/00-project-initialization.md`` §8 for the dependency-direction
rule this package exists to enforce.
"""
