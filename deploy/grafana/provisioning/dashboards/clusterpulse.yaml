# Loads every dashboard JSON in /var/lib/grafana/dashboards.
apiVersion: 1

providers:
  - name: ClusterPulse
    folder: ClusterPulse
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
