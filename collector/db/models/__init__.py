"""SQLAlchemy ORM models. Distinct from ``shared.contracts`` wire models by
design — see ``docs/architecture/00-project-initialization.md`` §9.3: the
wire contract and the storage schema change for different reasons and must
not be collapsed into one class.
"""
